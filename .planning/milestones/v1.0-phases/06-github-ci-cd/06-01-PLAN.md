---
phase: 06-github-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [CI-01, CI-02]
must_haves:
  truths:
    - "Every push to main and every PR triggers CI checks (frontend type-check + Rust clippy + cargo test)"
    - "Tag pushes matching v* create a draft GitHub Release with Windows NSIS installer"
    - "Frontend checks run on ubuntu-latest to save runner minutes"
    - "Rust checks run on windows-latest because Win32 crate requires Windows"
    - "Rust build artifacts are cached via swatinem/rust-cache"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with frontend and rust jobs"
      contains: "cargo test"
    - path: ".github/workflows/release.yml"
      provides: "Release workflow with tauri-action"
      contains: "tauri-apps/tauri-action@v0"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "src-tauri/Cargo.toml"
      via: "--manifest-path src-tauri/Cargo.toml"
      pattern: "manifest-path src-tauri/Cargo.toml"
    - from: ".github/workflows/release.yml"
      to: "GITHUB_TOKEN secret"
      via: "env injection for tauri-action"
      pattern: "GITHUB_TOKEN"
---

<objective>
Create GitHub Actions CI and Release workflows for the Diablo4 Build Applier.

Purpose: Automate compilation checks, linting, testing on every push/PR, and produce Windows NSIS installers on tagged releases.
Output: Two workflow files — ci.yml and release.yml — ready for GitHub Actions.
</objective>

<execution_context>
@/home/zhlkkk/.claude/get-shit-done/workflows/execute-plan.md
@/home/zhlkkk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-github-ci-cd/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/06-github-ci-cd/06-RESEARCH.md (workflow patterns and action versions)
    - src-tauri/Cargo.toml (verify manifest path)
    - tsconfig.json (verify tsc config exists)
    - package.json (verify npm scripts)
  </read_first>
  <action>
Create `.github/workflows/ci.yml` with two jobs:

**Job 1: `frontend` on `ubuntu-latest`**
- `actions/checkout@v4`
- `actions/setup-node@v4` with `node-version: lts/*` and `cache: npm`
- `npm ci`
- `npx tsc --noEmit` (type-check)

**Job 2: `rust` on `windows-latest`**
- `actions/checkout@v4`
- `dtolnay/rust-toolchain@stable`
- `swatinem/rust-cache@v2` with `workspaces: './src-tauri -> target'`
- Step "Clippy lint": `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings`
- Step "Unit tests": `cargo test --manifest-path src-tauri/Cargo.toml`

Triggers:
```yaml
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
```

Workflow name: `CI`

Do NOT include `cargo test -- --ignored` (integration tests hit external API, excluded from CI per project convention).
Do NOT include `cargo check` (clippy already compiles the code, check is redundant).
Do NOT use `actions-rs/cargo` (deprecated).
  </action>
  <verify>
    <automated>cat .github/workflows/ci.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>/dev/null || python3 -c "import sys; print('yaml module not available, checking with grep'); sys.exit(0)" && grep -q "cargo test" .github/workflows/ci.yml && grep -q "cargo clippy" .github/workflows/ci.yml && grep -q "npx tsc --noEmit" .github/workflows/ci.yml && grep -q "windows-latest" .github/workflows/ci.yml && grep -q "ubuntu-latest" .github/workflows/ci.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and is valid YAML
    - File contains `name: CI`
    - File contains `on:` with `push:` and `pull_request:` triggers on `branches: [main]`
    - File contains job `frontend` with `runs-on: ubuntu-latest`
    - File contains job `rust` with `runs-on: windows-latest`
    - File contains `actions/checkout@v4` in both jobs
    - File contains `actions/setup-node@v4` with `node-version: lts/*` and `cache: npm`
    - File contains `npm ci`
    - File contains `npx tsc --noEmit`
    - File contains `dtolnay/rust-toolchain@stable`
    - File contains `swatinem/rust-cache@v2` with `workspaces: './src-tauri -> target'`
    - File contains `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings`
    - File contains `cargo test --manifest-path src-tauri/Cargo.toml`
    - File does NOT contain `--ignored` (no integration tests in CI)
  </acceptance_criteria>
  <done>CI workflow file exists with frontend (ubuntu) and rust (windows) jobs, triggering on push/PR to main</done>
</task>

<task type="auto">
  <name>Task 2: Create Release workflow</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .planning/phases/06-github-ci-cd/06-RESEARCH.md (release workflow pattern and tauri-action config)
    - src-tauri/tauri.conf.json (verify Tauri config location for action auto-detection)
    - package.json (verify @tauri-apps/cli devDependency exists)
  </read_first>
  <action>
Create `.github/workflows/release.yml` with one job:

**Job: `build-and-release` on `windows-latest`**

Permissions block at job level:
```yaml
permissions:
  contents: write
```

Steps:
- `actions/checkout@v4`
- `actions/setup-node@v4` with `node-version: lts/*` and `cache: npm`
- `dtolnay/rust-toolchain@stable`
- `swatinem/rust-cache@v2` with `workspaces: './src-tauri -> target'`
- `npm ci`
- `tauri-apps/tauri-action@v0` with:
  - env: `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}`
  - with:
    - `tagName: v__VERSION__`
    - `releaseName: 'Diablo4 Build Applier v__VERSION__'`
    - `releaseBody: 'Windows installer for Diablo4 Build Applier.'`
    - `releaseDraft: true`
    - `prerelease: false`

Triggers:
```yaml
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'
```

Workflow name: `Release`

Do NOT add `projectPath` (tauri-action auto-detects `src-tauri/tauri.conf.json` by convention).
Do NOT add code signing config (deferred per research open question).
  </action>
  <verify>
    <automated>grep -q "tauri-apps/tauri-action@v0" .github/workflows/release.yml && grep -q "GITHUB_TOKEN" .github/workflows/release.yml && grep -q "contents: write" .github/workflows/release.yml && grep -q "releaseDraft: true" .github/workflows/release.yml && grep -q "workflow_dispatch" .github/workflows/release.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release.yml exists and is valid YAML
    - File contains `name: Release`
    - File contains `on:` with `workflow_dispatch:` and `push: tags: - 'v*'`
    - File contains `permissions:` with `contents: write`
    - File contains `runs-on: windows-latest`
    - File contains `actions/checkout@v4`
    - File contains `actions/setup-node@v4` with `node-version: lts/*` and `cache: npm`
    - File contains `dtolnay/rust-toolchain@stable`
    - File contains `swatinem/rust-cache@v2` with `workspaces: './src-tauri -> target'`
    - File contains `npm ci`
    - File contains `tauri-apps/tauri-action@v0`
    - File contains `GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}`
    - File contains `tagName: v__VERSION__`
    - File contains `releaseName: 'Diablo4 Build Applier v__VERSION__'`
    - File contains `releaseDraft: true`
    - File does NOT contain `projectPath` (auto-detection)
  </acceptance_criteria>
  <done>Release workflow file exists, triggered by v* tags and manual dispatch, produces draft GitHub Release with Windows installer via tauri-action</done>
</task>

</tasks>

<verification>
- Both workflow files exist in `.github/workflows/`
- Both are valid YAML syntax
- CI workflow has two jobs: frontend (ubuntu) and rust (windows)
- Release workflow uses tauri-apps/tauri-action@v0 with correct permissions
- No integration tests (--ignored) included in CI
- Rust cache configured with correct workspace path in both workflows
</verification>

<success_criteria>
- `.github/workflows/ci.yml` triggers on push/PR to main with frontend type-check and Rust clippy+test
- `.github/workflows/release.yml` triggers on v* tags with tauri-action producing draft release
- Both workflows use swatinem/rust-cache@v2 for build speed
- Frontend checks run on ubuntu-latest, Rust checks on windows-latest
</success_criteria>

<output>
After completion, create `.planning/phases/06-github-ci-cd/06-01-SUMMARY.md`
</output>
